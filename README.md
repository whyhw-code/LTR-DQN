- # Reproducibility Package README

  ## 1. General Information

  - **Paper Title**: Enhancing Predictive Selection of Sell-Side Analyst Reports via Learning to Rank and Reinforcement Learning
  - **Manuscript ID**: IJF-D-24-00575R3
  - **Authors**: Jiaming Liu, Hongyang Wang, Yongli Li, Kaiwei Jia
  - **Corresponding Author Email**: liyongli@hit.edu.cn
  - **Date of Packaging**: 2026.04.13

  ------

  ## 2. Repository Structure

  - `code/data/`: raw data
  - `code/model/`: reinforcement learning models
  - `result/`: LTR-DQN results
  - `code/temp/`: intermediate results

  ------

  ## 3. Computational Requirements

  ### 3.1 Software Requirements

  - Python version: 3.9.16
  - PyTorch: 2.0.0
  - CUDA: 11.7 (if GPU is used)

  ### 3.2 Dependencies

  See:

  - `requirements.txt`
  - `environment.yml`

  Installation:

  ```bash
  conda env create -f environment.yml
  conda activate torchtest
  ```

  or

  ```bash
  pip install -r requirements.txt
  ```

  ------

  ## 4. Data Description

  ### 4.1 Data Sources

  The data used in this study are derived from the Sell-Side Analyst Reports.

  ### 4.2 Data Availability

  The data are publicly available and can be downloaded from Eastmoney.

  ### 4.3 Data Files and Format

  The data consist of Sell-Side Analyst Reports files in PDF format.

  ------

  ## 5. Intermediate Data

  The intermediate data consist of Excel files compiled from information extracted from the analyst reports.

  ------

  ## 6. Code Description

  ### 6.1 Main Scripts

  - `DQN_train.py`: DQN training script (used if model retraining is required)
  - `dl_dqn2.py`: LTR-DQN model procedures
  - `T*.py`: scripts for generating tables in the paper

  ------

  ## 7. Table and Figure Reproduction

  ### 7.1 Script Naming Rules

  The scripts used to generate table results in the paper follow a unified naming convention.

  #### (1) General Tables (Table 3, Table 4, Table 5, Table 7, etc.)

  Naming format:

  ```text
  T[table number][market][row number].py
  ```

  - `T`: indicates Table
  - Table number: corresponds to the table number in the paper (e.g., `T4` denotes Table 4)
  - `C`: ChiNext market
  - `M`: Main Board market
  - Row number: corresponds to the specific row in the table

  Examples:

  - `T4C3.py` → Table 4, ChiNext market, Row 3
  - `T5M12.py` → Table 5, Main Board market, Row 12

  #### (2) Special Rules for Table 6

  Naming format:

  ```text
  T6[market][ratio]_[model].py
  ```

  - `C`: ChiNext market
  - `M`: Main Board market

  Ratio mapping:

  - `5` = 50%
  - `6` = 60%
  - `7` = 70%
  - `8` = 80%
  - `9` = 90%

  Model identifiers:

  - `_1`: LambdaRank
  - `_2`: LambdaMART + LTR-DQN

  Examples:

  - `T6C5_1.py` → Table 6, ChiNext, 50%, LambdaRank
  - `T6M6_2.py` → Table 6, Main Board, 60%, LambdaMART + LTR-DQN

  ### 7.2 Mapping Between Code and Results

  The correspondence between tables in the paper and scripts is as follows:

  - Table 3: generated by `T3*.py`
  - Table 4: generated by `T4*.py`
  - Table 5: generated by `T5*.py`
  - Table 6: generated by `T6*_*.py`
  - Table 7: generated by `T7*.py`

  Where:

  - `C` denotes ChiNext market
  - `M` denotes Main Board market

  Each script generates one row or one column of results in the corresponding table.

  ------

  ## 8. Instructions to Reproduce Results

  ### 8.1 Environment Setup

  It is recommended to use a conda environment:

  ```bash
  conda env create -f environment.yml
  conda activate torchtest
  ```

  or use pip:

  ```bash
  pip install -r requirements.txt
  ```

  ### 8.2 Data Preparation

  Please place the raw data in:

  ```text
  data/
  ```

  For details on data sources and acquisition methods, see Section 4.

  ### 8.3 Pre-trained Models

  Reproducing the results in this study does **not require retraining the model**.

  The relevant models have already been trained. For reproduction, directly use the existing trained results, intermediate results, or model files provided in the project.

  Therefore:

  - `DQN_train.py` is not a required step for reproducing the table results
  - Unless necessary, the training scripts do **not need to be executed**
  - To reproduce table results, directly run the corresponding `T*.py` scripts

  ### 8.4 Reproducing Table Results

  Run the corresponding scripts as needed, for example:

  ```bash
  python T4M1.py
  python T4M2.py
  python T4C1.py
  python T4C2.py
  ```

  or:

  ```bash
  python T6M5_1.py
  python T6M5_2.py
  ```

  Executing the corresponding scripts will generate the results for the respective tables.

  ### 8.5 Notes

  - All scripts are recommended to be run from the project root directory
  - Ensure that data paths are correctly configured
  - Ensure that intermediate files and pre-trained results are available
  - Model retraining can be performed if necessary, but it is not required for reproducing the results

  ------

  ## 9. Expected Runtime

  Under the recommended environment, the approximate runtime is as follows:

  - Single table generation: approximately 30 seconds (for Table 6, a single script may take 15 minutes to 1 hour)
  - Full reproduction of all tables: approximately [待填写]

  Actual runtime depends on hardware configuration.

  ------

  ## 10. Hardware Requirements

  The recommended hardware configuration is as follows:

  - CPU: multi-core processor (4 cores or above recommended)
  - Memory: at least 16GB (32GB recommended)
  - GPU: NVIDIA GPU supporting CUDA 11.7 recommended
  - Python: 3.9.16

  Notes:

  - The project runs more efficiently on a GPU environment
  - Execution on CPU may be slower (to be confirmed whether fully supported)

  ------

  ## 11. Additional Notes

  - The `temp/` directory stores intermediate results
  - The `result/` directory stores model output results
  - The `model/` directory contains model definitions or training-related files
  - The `end/` directory stores outputs of baseline models
  - It is recommended to follow the README instructions when executing scripts
  - If path errors occur, please check the current working directory
  - Minor discrepancies in results may arise due to randomness or hardware differences
